export type Project = {
  title: string;
  description: string;
  technologies: string;
  githubUrl: string;
  liveUrl?: string;
}

export type Certificate = {
  title: string;
  institution: string;
  year: string;
  certificateUrl: string;
}
